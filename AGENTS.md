# 风筝单词工作约定

## 产品与边界

- 产品名为“风筝单词 / KiteDance”，生产域名 kitedance.com。当前架构是 React 静态前端 + Cloudflare Worker API + D1 课堂数据 + R2 独立素材；本地仅用 Wrangler 模拟同一 API 测试。Python 保留教材编译、历史 SQLite 迁移与旧发行包参考。A4 打印与大屏授课不能回退。
- 在本仓库内工作，先读 README、入口、相关测试和 `git status`；保留已有修改，不操作父目录的其他项目。
- 流程固定为：班级 → 新建课程 → 词表/难度/数量 → 确认并编辑词单 → 持久化课程。词表范围可以多选，候选池按英文规范化拼写去重并优先采用先选词表的条目；默认 10 词，可选 20、30、50。手动补词可以跨词库，单词与词组使用同一结构；不同课程可以重复选择同一个词，同一节课内仍只保留一次。
- 随堂书写纸与课后考察练习是独立的 A4 文档，都能课前打印，保留姓名、年龄、日期填写处。
- “材料打印”还包含独立的双面单词卡片，每张 A4 纵向排 2 列 × 3 行。正面英文单词与英文例句，背面中文释义、译句与简明解释；按长边翻转左右交换背面列，先补齐末页空位再镜像，页面顺序为正 1 / 反 1 / 正 2 / 反 2。正反面必须复用同一课程版本词序，不能重新抽词，不能引入冗长词源。打印设置提示 100% 实际大小、关闭页眉页脚，保留裁切线与核对序号；涉及卡片改动运行 `npm run test:print` 和隔离的 Cloudflare 打印接口测试。
- “材料打印”的情景故事复用当前版本的英文正文和公共配图，每页一个情节，长正文续页；不印中文标题、译文或中文图片说明。故事中的目标词可加粗，保留英文 Name / Age / Date 填写处。课程内打印和单独打开打印都须等待字体、图片准备完成；加载失败明确提示并可重试。所有打印类别的每一页都显示 `kitedance.com`，不要依赖浏览器自动页脚。
- 教师界面以课程内容为主：主页不放宣传话术，侧栏主要显示课程列表，学生管理/词库管理/班级进度用一排有可访问名称和提示的图标。课程操作集中到紧凑顶部，课堂笔记在右上角弹窗编辑；复制、保存等短提示使用浮动 toast，表单校验可在字段附近显示。课前语音面板关闭不能中断缓存，换课时须取消旧课任务。
- 输入框、文本域、下拉框、复选框和单选框不显示浏览器 `outline` 或额外的 focus ring；焦点状态只通过控件本身的边框颜色反馈，避免外围光圈干扰页面布局。
- 进入专用输入步骤或编辑弹窗时，自动聚焦唯一的主输入框；学生拼写切题、课堂构词换题和提示操作后要回到答案框，课堂填空聚焦第一道未答题。常驻筛选器、只读字段和同屏多行表格不得在页面打开时抢焦点。
- 投屏中使用的主输入框字号至少 20px，并随桌面屏幕适度放大；课堂填空、构词快问和学生拼写等需要全班观看的答案框使用约 24–32px 或更大的响应式字号，同时增加控件高度和行高，避免文字裁切。后台密集表格与常驻筛选器保持紧凑。
- 单词卡片正面及课堂翻卡的英文面，在英文下显示音标与真实构词成分，各成分用不同颜色。整体词标明整体记忆，缺少可靠拆分时不硬拆。打印例句中的目标词及已收录的正确词形加粗标记；卡片四周用虚线，横纵留 6mm 空隙，正反面用同样的尺寸和间距。
  课堂翻卡只展示词汇、释义、音标和构词等学习信息，去掉“说出中文”“点击翻面”“你答对了吗”等可见提示语；保留必要的朗读、翻转和掌握情况操作及可访问标签。打印卡片必须显式隔离交互卡片的高度、阴影和变换样式；中文记忆解释正文使用至少 15px 字号。涉及排版时同时验证内嵌预览和独立打印的实际 A4 裁剪间距，不能只检查 CSS 的 gap 数值。
- 侧栏不再重复班级名称；课程步骤 tab 等宽排列，不创建横向或纵向滚动容器。当前版本的课堂笔记在结课后仍可单独更新，不能借此修改已归档的教学内容或掌握情况；旧版本快照只读。
- 班级首页提供复制入口：只复制所有未删除课程的最新内容，保留课序、题序、词库 ID、单词素材 ID/URL 与故事引用；为副本生成新班级、课程和版本编号。每课重置为未结课、版本 1、首个步骤，清空笔记、答题、掌握与播放进度；不复制学生、选词草稿或历史版本。素材是公共资产，复制/删除均不重建或删除 R2 对象。复制请求须幂等且所有课堂写入在同一 D1 事务中完成。
- 构词快问支持一个或多个英文举例，用逗号/换行分隔，保留词组空格；按完整拼写在全部词库查询。分别展示词库是否收录、构词证据是否匹配与本课参考答案，不能按包含相同字母判同根，也不能将词库未收录直接当成不存在的英语单词。查询不写学习进度。相关改动运行 `npm run test:classroom` 与隔离的 Cloudflare 测试。
- 讲课面向学生，大屏幕单页播放，避免备课口吻。课程尾部按页完整回顾所有单词、释义和例句；保留翻页、断点继续、临时笔记与互动。

## 先定课程，再制作素材

1. 先创建并确认词单。记录页面上的 `WG-…` 课程编号；编号唯一映射到 D1 中的课程版本，不使用容易混淆的“第几课”定位。
2. 用页面的“下载素材任务”或 `python3 scripts/cf_course.py brief WG-…` 读取固定内容。不要为方便画图重新抽词。
3. 检查 `assets/catalog.json`，优先复用词义匹配的已有素材。每个单词支持一张或多张图片；没有图片时授课位置留空，不显示虚假图片或占位文案。
4. 新素材放入 `assets/words/<word>/vN/`；跨词故事放入 `assets/lessons/<bundle-id>/`。图片、文字、原始生成提示词、生成方式、日期、词义、来源与 SHA-256 必须一起提交 Git。不要仅保存模型临时路径或外链。
5. manifest 中使用单词、级别和素材 ID，不依赖某台机器的词库数字 ID。课程包必须声明固定 `word_order`，故事声明实际覆盖的 `covered_words`。
6. 用 `python3 scripts/lesson_assets.py validate` 检查，先 `npm run cf:deploy` 发布素材，再用 `python3 scripts/cf_course.py apply WG-… <bundle-id>` 绑定。绑定创建新的课程版本，旧内容、题序和素材文件应可回溯；不要直接改历史课程快照。
   交付前核对课程页、侧栏与打印材料的显示标题是否为已确认的课程主题；内容包的标题不会自动覆盖创建时的课程名称，不能遗漏“第二课”这类临时名称。仅修正名称时保留课程编号、词单、素材、学习进度和历史版本。
7. 后续改稿新增 v2 等目录和素材 ID，不覆盖已发布的图片或 manifest。图片用内容哈希命名；同一图片可以被多个课程引用。`catalog.json` 只负责为新课程选择默认素材，不改写旧课。

## 图片与语言标准

- 图片必须精美、有趣、以写实摄影质感为主；自然光、明快颜色、清楚的主体、可信的动作和材质，适合小学生及大屏幕观察。
- 用真实场景说明词义。抽象词可用前后对照或人物行动，不为了配图偷偷换成别的词义。每个被要求制作的单词至少一张可用图片，不用 CSS、SVG 或 emoji 冒充生成图片。
- 图片内不烧录单词、对话、标签或水印。文字由网页排版，便于高亮、朗读、显示译文与以后改稿。默认横向 3:2，主体不贴边。
- 同一故事人物的服装、外貌、关键道具和动作前后保持一致。故事图文简洁明快，每页一个情节，通常 20–40 个英文词、2–4 页，有一个小问题或可表演的对话。
- 情景故事放在“认识单词、构词线索”之后、“课堂互动练习”之前。覆盖主要目标词即可，不为凑齐每个词牺牲自然程度。目标词以外优先用 A1/A2 常见词，必要的陌生词提供中文线索。
- 补充例句要自然、短、符合课程级别；练习答案应能从题干或提示判断。释义、词性、图片和例句必须对应。
- 后续课程主要由大模型在课前准备。使用 `word_study.schema_version=2`，来源提示 `origin_zh` 可省略或留空；只有背景有趣、可靠且直接帮助理解或记忆时才写一两句（最多 60 字）。不要为填满字段罗列古英语/拉丁语拼写、年代、生僻演变或与当前词义无关的历史；没有帮助就不展示。学生视角只保留清楚的构词与常用关联词，可靠出处存入 sources。旧 v1 的冗长词源保留在快照中但不在教学页展示。
- 构词只教真实可解释的联系；区分词根、词基、词缀和合成词。不能硬拆每个词。词源、年代和历史故事须查可靠词典并记录来源；联想故事明确作为记忆画面，不冒充词源事实。
- 音标显示统一通过 `lib/word-presentation.ts`；词级公共校订记录放在 `data/pronunciations.ts` 并附可靠词典来源，跨课复用，不改写历史课堂快照。优先使用经核对的音标，再读取词表音标；缺失时不可猜测。
- 新图片使用内置 imagegen；保存最终图片及完整提示词。外部图片必须记录原始来源与复用授权，不批量抓取来历不明的图片。

## 数据、交付与验证

- 语音使用 Edge 在线神经语音；Worker 的 WebSocket 适配在 `worker/audio.ts`，本地素材制作使用 `edge-tts`。默认 `en-GB-SoniaNeural`、语速 `-10%`，不需要 API Key。用户已要求删除 GPT API 通道，不再添加或保留 GPT 配音配置，不使用 Codex 登录凭证调用其他 API，也不自动退回系统合成声音。
- 单词、完整例句、关联词、故事正文与问答共用 `worker/audio.ts` 和 `lib/audio.ts`，前端不要直接请求第三方语音 URL。先检查 R2 缓存，未命中才联网获取；本地素材制作可由隔离的 `.venv-audio` 工作进程生成。文本、声音、语速和格式参与缓存键，MP3 以内容哈希命名。
- `assets/audio/v1/` 内的 MP3 及 JSON（原文、生成方式、声音参数、日期、来源、SHA-256）一起进 Git；不保存班级名、学生信息、课堂笔记或答题记录。相同文本跨课复用，不改课程快照。每轮任务提交新增的公共教材录音；网页本身不自动执行 Git 命令。
- 保留“课前缓存本课语音”、进度、暂停/继续、例句和故事播放入口。缓存中断不丢已有录音，失败或损坏文件不能算缓存成功；翻页、切课、退出讲课必须停止播放并取消过期请求。页面注明“AI 合成语音”。
- “预下载课程资源”与云端语音准备分开：手动将当前版本的全部单词/故事图片及已有录音存入浏览器 Cache Storage，按哈希 URL 去重，完整校验后才计成功。缓存有效期 7 天，关闭弹窗继续、切课/刷新中止未完成任务，重试只补缺失文件；缺失的云端录音须明确提示，不能声称全课资源已齐。Service Worker 仅接管同源哈希素材 GET/HEAD，支持本地音频 Range；不缓存 HTML、API、登录、课程或学生数据，不改云端独立素材。只清理本功能自己的过期缓存，不动旧产品缓存。相关改动运行 `npm run test:resources`、音频测试及本地浏览器的刷新复用/断网素材播放验证，不宣传整站离线可用。
- 语音相关改动额外运行 `npm run test:audio`、`python3 scripts/lesson_audio.py validate`。自动测试不调用在线语音服务，使用临时素材目录和数据库；实际生成录音后验证文件可解码、缓存命中、R2 复用与 Cloudflare 实际运行。WebSocket 必须在 accept 前显式设 binaryType="arraybuffer"，不要依赖旧默认值。

- `assets/`、词表、内容包、生成脚本与文档进入 Git；学生资料、班级记录、SQLite、D1 导出、备份、密钥、Wrangler 状态、临时文件和发布 ZIP 留在忽略目录。清课仅在用户要求时执行，先用 SQLite backup API 备份，保留词库；本项目不在启动或发布时清库或自动创建演示课；删除班级/课程采用软删除，不删除任何独立单词资产。
- 支持在全新数据库显式运行 `create-demo` 重建仓库里的示例课程；普通启动只导入词表。
- 按改动运行 `npm run typecheck`、`npm run lint`、`npm test`、`npm run test:slides`、`python3 scripts/lesson_assets.py validate`、`npm run build`。Cloudflare 改动还须运行 `npm run test:cloudflare`；该测试必须在临时独立 D1/R2 状态目录执行。涉及旧离线包时才运行对应发布 smoke。
- 验证素材完整性、路径约束、课程词单不变、历史快照不变、无图片课程可用、故事章节顺序和分页；不得用真实课堂数据库或生产 D1 跑自动化破坏性测试。
- 每完成一轮本项目任务，在相关验证通过后，必须提交本轮代码、素材与文档变更，并 push 到 GitHub 仓库 `git@github.com:royguo/words_hero.git`（当前为 `origin/main`）。这是用户持续授权的默认收尾步骤，无需再次确认；只提交本轮范围内应追踪的文件，继续排除本地数据库等私人数据。
- 推送后核实远端已包含本轮提交，汇报提交哈希和推送结果。若本轮没有可提交的文件变更，核实并同步已有提交，不创建空提交；若推送失败，说明原因，不把仅完成本地提交说成已同步 GitHub。
- 完成后保留 Wrangler 本地预览服务，按任务内容说明课程编号、素材路径和验证结果。

## 云端更新、教材与账号

- 部署前阅读 [docs/cloudflare.md](docs/cloudflare.md)。`npm run cf:deploy` 自动执行未执行迁移、R2 上传和词库 upsert，再发布 Worker；绝不把本地测试课堂默认导入生产。新迁移追加编号，不改已发布迁移。
- 每轮提交前先 `npm run assets:pull` 收回线上新录音，验证 MP3 和 manifest，再提交 Git。R2 与词库均独立于班级/课程，删除课堂不影响资产。
- 逐词小故事已退役。“认识单词”紧接“来源和构成”；精编词填写 `word_study`，详见 [docs/word-study.md](docs/word-study.md)。课堂题目与答案一次性存入版本 `config.worksheets`，打印时不重新抽题。
- 默认教师账号 admin / 95279527，生产凭据存 Worker secrets；记住登录使用 HttpOnly 会话 Cookie，不存明文密码。学生账号只由老师手动创建；默认账号为北京时间当天日期 YYYYMMDD + 全站递增序号（至少三位，如 20260806001），初始密码为账号后六位。保存时按当日已有最大编号分配，包含已移除账号，并处理并发冲突；旧账号不改名。服务端按角色和学生 ID 限制权限，学生不能访问班级管理、教师课程接口、备份或他人记录。
- 部署与切换 kitedance.com、每轮 commit & push 是用户持续授权，正常更新无需重复申请。保留旧产品资源及回退路径，不扩展到无关子域名或数据库。

## 学生练习规则

- 班级侧栏保留“学生管理”和暂未开放的“班级进度”；班级删除入口仍在所有班级页。学生管理使用可快速编辑的表格，姓名必填，账号/密码自动生成且可编辑，手机号选填。保存密码用 PBKDF2，不回显旧密码；可重设为账号后六位。账号或密码改变即撤销旧学生会话。
- 首页以 tab 选择老师/学生登录；学生进入 `/learn`。两端均保持 30 天登录，权限必须由 Worker 校验，不能只靠隐藏按钮。
- 学生可学词汇 = 所属未删除班级中，每节未删除课程的 **最新已结课版本** 的词汇并集。草稿、进行中的课、被替代的旧版本不贡献词汇。老师点击结束课程才开放。已结课课程可创建新版本；确认新版本后，旧版本不再贡献，直到新版本再次结课。切勿为了演示自动替老师结课。
- 同一英文按规范化拼写去重，采用最近结课的释义。同组避免完全相同的中文标签，防止消消乐歧义；不足所选数量时使用实际数量；单词不足以构造听音干扰项时仍须检验拼写回忆，兼容模式保留单词双向判断。
- 到期词优先，其次未练词；到期词结合相对逾期时长、间隔级别和错词次数排序，优先巩固更弱的词。默认每组 10 词，可选 5 词；间隔规则统一位于 `lib/student-game.ts`。当前无错复习间隔为 10 分钟、1/3/7/14/30/60 天，错词回到第 0 档并进入重新巩固，5 分钟后再练；提示后答对 10 分钟后再练，重新巩固成功后先安排次日，不能跳回原来的长间隔；这是可调整的间隔练习策略，不宣传为对所有人精确适用的艾宾浩斯定律。提前加练且全对不提升间隔，避免刷熟练度。
- 兼容旧版“日常练习”的状态机 v2：答错后持久保留纠错卡，展示正确的英文、中文、已有词图与短例句，支持朗读，点击继续才恢复答题。不得定时关闭纠错或自动跳入重试。通过部分保留，只补练未通过的词/方向。
- 兼容 v2 的阶段 1：中英消消乐。错配涉及的两个单词的所有卡片都消失，两个词分别记错；正确配对移除一词并保留通过记录。本轮结束后仅补练未通过的词，全部词通过才进入阶段 2。
- 兼容 v2 的阶段 2：每词分别验证英文/中文两个方向，答错时题干和候选涉及的两词分别记错（同一词只记一次），清除这两词本阶段的方向通过记录。查看纠错后先做剩余题，本轮结束只补缺失的方向，干扰项仍可来自整组。不会退回阶段 1。
- 可选“挑战模式”保留原 v1 整轮全对规则；部署前已经开始的 v1 会话继续原规则，不重写旧历史。新前端创建练习显式传入 mode=adaptive/challenge；旧客户端 mode=practice 仍使用 v2；旧客户端不带 mode 时继续 v1 兼容。纠错确认、补练都属于阶段内本地动作，不逐题请求数据库。
- 学生进度显示初识、巩固中、已记熟、到期词和积分；点击查看个人逐词间隔轨迹、历史练习和积分明细。逐词记忆始终按 student_id + word_key 保存；曲线展示实际复习间隔与事件，不制造精确遗忘百分比。已移出课堂的词保留个人历史，但不能继续出题。
- 新完成练习用 policy_version=2：step >= 4 且有隔至少 7 天、无提示拼写回忆证据、不在重新巩固才视为“记熟”。会话创建时间由服务器提供，延迟上传不能充当跨天证据；未再次练习的旧记录保持原标记。首次到达奖励默认 10 积分，老师按班级设为 0–1000；提前加练不升级。同一学生、同一规范化词只记录一次首次记熟奖励，重学、重复结课、删课再加均不能刷分。遗忘不自动扣除已有积分。0 分规则生效时仍记录首次记熟，此后调价不追补；新的首次记熟奖励须满足新版标准；已发积分不追扣。不得启动或部署时修改旧记忆进度。
- 学生管理提供积分余额、奖励加分、兑换抵扣、设置余额与明细，填写原因并展示调整前后余额。积分规则变化仅影响之后首次记熟的奖励；已发积分不回算。客户端不决定记熟/所得积分，完成整组时 Worker 复算后将记忆、逐词历史与积分在同一 D1 事务保存。积分流水插入触发余额变更，数据库禁止负余额；教师写入校验余额版本并使用稳定请求 ID 防止重复抵扣。学生只能看自己的记录，不能改分或访问他人记录。
- 逐题在前端立即判定和反馈，不等待网络。阶段内错误重试也在本地进行；阶段结束后在后台统一提交动作列表，不等待网络再显示下一题；冻结在途请求的 ID/动作，后续答案追加到尾部，回包只确认已发送的前缀，主动返回/退出时保存未完成部分，极长练习最多每 1000 个动作保存一次。浏览器按学生隔离暂存未同步动作，刷新可重放；跨设备从最后同步处继续。Worker 以相同的确定性题序重放动作并核算错词，不信任客户端分数；请求 ID 防重复、版本号防并发覆盖。同步失败不锁住本组答题；保留原请求，联网或定时重试。刷新需恢复服务端已提交、回包丢失期间产生的本地尾部；并发冲突回到权威进度。完成一组才原子更新个人间隔，新一组等待最终同步确认。课堂词表改变后旧练习失效，个人历史保留；删除课/班/学生不删除素材或记忆历史。
- 页面以专注为先，不加宣传语。词义热身、兼容模式与纠错中的英文自动朗读，使用 R2 已有录音；拼写回忆禁止朗读隐藏答案，听音题不提前展示英文；换题、退出、切课要取消过期音频。浏览器阻止自动播放时提供“朗读”按钮，不妨碍答题。成功/失败音效由本地 Web Audio 合成（源码可追踪），可关闭；动画支持 reduced-motion。
- 学生点击开始或继续练习后进入与课堂播放一致的单屏模式：浏览器支持时由点击手势进入全屏，退出系统全屏后仍铺满当前视口；页头、进度、题目、纠错和完成页都不能依赖页面或题目区纵向滚动。桌面、手机及矮屏分别验收。错配纠正卡的词图必须等比例完整显示，使用 `object-fit: contain`，不能为填满卡片裁切记忆主体。
- 学生每次开始、继续或进入下一组前显示全屏资源进度门：为本组单词和例句准备音频，并将已有词图和录音按内容哈希校验后写入浏览器 Cache Storage。默认等全部资源成功后显示题目；同时提供“跳过下载，直接开始”，点击后立即取消当前剩余预下载并放行本组，声音和图片继续按普通联网方式加载。下载失败可选择重试或跳过，每个新组重新显示选择，已经完整缓存的公共素材跨组复用。该流程只准备资源，不逐题写数据库，答题仍按阶段边界批量保存。
- 学生功能验证：`npm run test:students`、`npm run test:cloudflare`，后者在临时独立 D1/R2 中覆盖注册编辑、权限隔离、最新结课版本、两阶段错误重试、幂等恢复、记忆调度与删除。不得在生产真实班级中跑这些测试。新增迁移只追加文件，不改既有迁移。

- 默认 v3 自适应日常练习：按学生本人词义/听音/拼写的独立、提示、错误证据一次性生成题目；薄弱词义先热身（每屏最多 5 对），再按弱项安排中文回忆与听音题，熟悉词不重复整套配对。提示按动作记录；补练答对不能抹去原错误。纠错后优先隔 3 题再回测，每题最多一次补练，也可留待下次，不能无限重来。完成任务不等于记熟所有词。具体数据与兼容规则见 docs/student-learning.md。
