# 风筝单词品牌图 · v2

亮蓝色风筝、打开的书与橘橙色尾结。保留 v1 的图案身份，以白色图标底面和暖白书页分隔增强深色侧栏中的辨识度；中英文字标仍由网页原生文字排版。

- `logo-mark.png`：最终 AI 编辑原件，1254 × 1254 RGB PNG，近白色不透明背景。
- `source-color-edit.png`：第一次换色编辑的原始输出，含烘焙棋盘格，仅留作制作记录，不用于网站。
- `color-edit.prompt.txt`、`white-background.prompt.txt`：两次编辑的完整提示词。
- `*.metadata.json`：生成记录与像素检查；`provenance.json`：集成信息、衍生文件与 SHA-256。
- `public/favicon.png`（256 px）、`public/logo.png`（512 px）：使用 macOS sips 等比缩小，网页图标使用版本查询参数更新缓存。

v1 文件不覆盖。最终背景是近白而非逐像素纯白，未对图像进行抠图或重绘；在网页的小尺寸白底图标上使用。
